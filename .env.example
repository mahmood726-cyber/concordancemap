# ConcordanceMap environment template.
# Copy to .env locally and fill only the values needed for your workflow.

# Optional for NCBI E-utilities; enables the higher authenticated rate limit.
NCBI_API_KEY=

# Required for TruthCert signing; use a real secret at least 16 bytes long.
CONCORDANCEMAP_HMAC_KEY=
